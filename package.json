{
    "name": "daedalus",
    "scripts": {
        "web:dev": "cd packages/web && bun --env-file=../../.env run dev",
        "web:build": "cd packages/web && bun --env-file=../../.env run build"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "@types/node": "^24.8.1",
        "prettier": "^3.6.2"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "@trpc/client": "^11.6.0",
        "@trpc/server": "^11.6.0",
        "zod": "^4.1.12"
    }
}
